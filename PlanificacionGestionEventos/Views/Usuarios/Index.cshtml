@model IEnumerable<PlanificacionGestionEventos.Models.Usuario>

<div class="container">

    <h2>Gestión de Usuarios</h2>

    <a asp-action="Create" class="btn btn-primary mb-3">
        Crear Usuario
    </a>

    <table class="table table-striped">

        <thead>
            <tr>
                <th>Nombre</th>
                <th>Email</th>
                <th>Teléfono</th>
                <th>Password</th>
                <th>Acciones</th>
            </tr>
        </thead>

        <tbody>

            @if (Model != null)
            {
                foreach (var item in Model)
                {
                    <tr>

                        <td>@item.NombreCompleto</td>

                        <td>@item.Email</td>

                        <td>@item.Telefono</td>

                        <td>@item.PasswordHash</td>

                        <td>

                            <a asp-action="Edit" asp-route-id="@item.UsuarioId" class="btn btn-success btn-sm">
                                Editar
                            </a>

                            <a asp-action="Details" asp-route-id="@item.UsuarioId" class="btn btn-info btn-sm">
                                Detalles
                            </a>

                            <a asp-action="Delete" asp-route-id="@item.UsuarioId" class="btn btn-danger btn-sm">
                                Eliminar
                            </a>

                        </td>

                    </tr>
                }
            }

        </tbody>

    </table>

</div>